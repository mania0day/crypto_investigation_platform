"""Canonical domain model, error hierarchy, and settings.

Everything above the chain adapters depends on this package and nothing else
in ``cipherchain`` — ``core`` itself imports no other ``cipherchain`` package.
"""
